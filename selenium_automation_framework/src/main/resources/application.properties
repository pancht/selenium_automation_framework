env=${env}
user.email=${user.email}
user.username=${user.username}
user.password=${user.password}
proxy.server.url=${proxy.server.url}
proxy.server.port=${proxy.server.port}
site.url=${site.url}
grid2.hub=${grid2.hub}
browser.name=${browser.name}
browser.version=${browser.version}
browser.platform=${browser.platform}
product.version=${product.version}
browser.loadCertificate=${browser.loadCertificate}
jenkins.screenshot.folder.url=${jenkins.screenshot.folder.url}
test.data.csv=${test.data.csv}
test.data.users.csv=${test.data.users.csv}